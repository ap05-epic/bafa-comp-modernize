# new-ui build/deps
new-ui/node_modules/
new-ui/dist/
# lockfile is NOT committed: the pod npm mirror lags public npm, so resolve fresh per-env
new-ui/package-lock.json

# checker temp + diff artifacts (regenerated each run)
**/*.diff.png
_baseline_capture.png
_candidate_capture.png
compare-result.json

# real session state -- NEVER commit
auth_state*.json

# python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db
