{
  "name": "new-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "1:1 React replica of the legacy BAA UI. The conversion loop fills this in, screen by screen.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.1.1"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  },
  "comment": "No package-lock is committed on purpose: the pod's npm mirror lags public npm, so each environment resolves fresh against its own registry. overrides pins transitives the mirror has not cached yet.",
  "overrides": {
    "nanoid": "<3.3.15"
  }
}
